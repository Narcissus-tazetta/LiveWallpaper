{
  "name": "livewallpaper-store-worker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.0",
    "@cloudflare/workers-types": "^4.20260701.0",
    "typescript": "^5.5.4",
    "vitest": "^3.2.0",
    "wrangler": "^4.64.0"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20",
    "fflate": "^0.8.3"
  }
}
